defmodule Nvims.Controller.Vswitch do
  def get(uuid \\ nil) do

  end

  def create(%{
    "name" => name
  }) do

  end

  def update(%{
    "uuid" => uuid,
    "name" => name
  }) do

  end

  def connect(%{
    "uuid" => uuid,
    "vm_uuid" => vm_uuid
  }) do

  end

  def disconnect(%{
    "uuid" => uuid,
    "vm_uuid" => vm_uuid
  }) do

  end

  def delete(%{"uuid" => uuid}) do

  end
end
